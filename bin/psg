#!/usr/bin/env python3

from string import ascii_letters, digits, punctuation
from secrets import choice

for a in range(6):
    length = range(int(choice(range(96, 102))))
    print("- " + "".join(choice(ascii_letters + digits + punctuation) for b in length))
