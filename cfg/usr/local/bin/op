#!/usr/bin/env bash
#
# Openssh helper
set -e

addr() {
  adb exec-out ip a | grep 'inet.*wlan0$' | cut -d ' ' -f6 | cut -d '/' -f1
}

case "${1}" in
  g) ssh-keygen -t ed25519;;
  c) ssh-copy-id -p8022 "$(addr)";;
  u) ssh -p8022 "$(addr)";;
esac
