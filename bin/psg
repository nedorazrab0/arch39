#!/usr/bin/env python3

import string
import secrets

for i in range(6):
    print("- " + ''.join(secrets.choice(string.ascii_letters + string.digits + string.punctuation) for i in range(int(secrets.choice(range(96, 102))))))

exit()
