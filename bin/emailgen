#!/usr/bin/env python3

from string import ascii_letters, digits
from random import choice

def gen(n):
    a = "".join(choice(ascii_letters + digits) for i in range(n))
    return a

if __name__ == "__main__":
    print(gen(2) + "." + gen(3) + "." + gen(3) + "  " + "@gmail.com")
